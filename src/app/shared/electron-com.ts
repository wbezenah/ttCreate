export enum WindowFunc {
    close = 'close',
    max = 'maximize',
    unmax = 'unmaximize',
    min = 'minimize'
}

export enum Channels {
    windowFunc = 'window-func',
    windowRes = 'window-result',
}